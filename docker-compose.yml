services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    container_name: media-blob-kit-fe
    restart: unless-stopped
    ports:
      - "${PORT:-5173}:80"
